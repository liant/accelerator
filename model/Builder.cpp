#include "Builder.h"
#include "Module.h"

Builder::Builder(Context *pContext):pContext(pContext)
{
    //ctor
}

Builder::~Builder()
{
    //dtor
}

void Builder::build(Module *pModule)
{
    //pModule->build(pContext);
}
